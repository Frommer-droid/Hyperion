[tool.pytest.ini_options]
minversion = "8.0"
addopts = "-ra"
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
line-length = 100
target-version = "py311"
exclude = [".venv", "build", "dist", "Build_Tools/build", "Build_Tools/dist"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]
